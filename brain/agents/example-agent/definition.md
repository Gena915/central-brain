# Example Agent — definition

**Section:** agents/example-agent
**Date:** seeded on install
**Tags:** example, agent, definition

## Goal
What this agent exists to do, in one sentence.

## Method
How it works: what it reads, what it produces, which model it runs on, when it's invoked.

## Outputs
Where its results land (a `results/` or `reports/` subfolder), so other skills can consume
them.

> Delete this folder once you have real agents — it's just the mold.
