# workspaces/ — the workshops

> **Raw** code and projects — repos, apps, datasets, bundles — that you edit, build, deploy
> or train. This is NOT distilled knowledge (that lives in the other buckets). It's the
> working material.

**Path:** workspaces/
**Last updated:** seeded on install

## Sections
- _(empty — e.g. `work/` and `personal/` sub-areas, or one folder per repo/app)_

## Note
Large or generated artifacts (node_modules, builds, datasets) should be gitignored. Keep
the brain about *knowledge and pointers*; the heavy raw material can live here but stays
out of version control via `.gitignore`.
